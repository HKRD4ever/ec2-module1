# Local .terraform directories
**/.terraform/*

# .tstate files
*.tfstate
*.tfstate.*
*.terraform.lock.hcl

# Crash log files
crash,logcrash.*log


# Exclude all .tfvars files, which are likely to
# password, private keys, and other secrers. theses
# control as they are data points which are potent
# to change depending on the environnement
*.tfvars
*.tfvars.json

# Ignore overide files as thes are usually used 
# are not checked in
overide.tf
overide.tf.json
*_overide.tf
*_overide.tf.json

# include overide files you do wish to add ver
# !example_overide.tf

# include tf plan files to ignore the plan output
# example: *tfplan*

# ignore CLI configuration files
.terraformc
terraform.rc
